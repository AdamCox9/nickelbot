/*****

	This should get all of the data from each exchange and store it locally.
	A flag can be set somewhere to automatically synchronize if haven't been already.
	And, have it automatically synchronize if it hasn't in the last delta amount of time.

 *****/